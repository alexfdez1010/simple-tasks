/** Maximum number of configurable cards on one statistics canvas. */
export const MAX_STATISTICS = 30;

/** Maximum categories rendered individually before folding the remainder. */
export const MAX_STATISTIC_CATEGORIES = 14;
